#include <stdio.h>

int main(){
 int  qtd, num;
 for (int i = 0; i <= 6; i++)
 {
    scanf("%d",&num);
    if (num > 0)
    {
       qtd = qtd + 1;
    }else{}
    
 }printf("%d valores positivos",qtd);
 
    return 0;
}
